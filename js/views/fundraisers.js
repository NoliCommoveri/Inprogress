// fundraisers.js — fundraisers + occurrences, progress bar.
// Stage 5 builds this out.
export function mount(container) {
  container.innerHTML = '<h2>Fundraisers</h2><p>Stage 5 builds this view.</p>';
}

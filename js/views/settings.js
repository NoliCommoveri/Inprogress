// settings.js — team name/season, backup status, export/import, "my player".
// Stage 6 builds backup/restore UI here; Stage 8 adds the range-export panel.
export function mount(container) {
  container.innerHTML = '<h2>Settings</h2><p>Stage 5 builds this view.</p>';
}

// schedule.js — unified games + practices, shared list/calendar.
// Stage 5 builds this out.
export function mount(container) {
  container.innerHTML = '<h2>Schedule</h2><p>Stage 5 builds this view.</p>';
}
